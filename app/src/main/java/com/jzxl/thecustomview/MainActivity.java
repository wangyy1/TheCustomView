package com.jzxl.thecustomview;import android.animation.ValueAnimator;import android.content.Intent;import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.util.Log;import android.view.View;import android.view.animation.LinearInterpolator;import android.widget.Button;import com.jzxl.thecustomview.widget.CountDownView;import com.jzxl.thecustomview.widget.StretchableFloatingButton;public class MainActivity extends AppCompatActivity {    private static final String TAG = "MainActivity";    private Button mButton;    private Button mButton1;    private CountDownView mCountDownView;    private StretchableFloatingButton mStretchableFloatingButton;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        mButton = findViewById(R.id.button);        mButton1 = findViewById(R.id.button1);        mCountDownView = findViewById(R.id.countDownView1);        mStretchableFloatingButton = findViewById(R.id.stretchableFloatingButton);        mButton.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                mCountDownView.start();                //这里是时间获取和赋值                ValueAnimator animator1 = ValueAnimator.ofInt(6, 0);                animator1.setInterpolator(new LinearInterpolator());                animator1.addUpdateListener(new ValueAnimator.AnimatorUpdateListener() {                    @Override                    public void onAnimationUpdate(ValueAnimator valueAnimator) {                        int time = (int) valueAnimator.getAnimatedValue();//                        Log.e(TAG, "onAnimationUpdate: " + time);                    }                });                animator1.start();                ValueAnimator animator = ValueAnimator.ofInt(50, 20);                animator.setInterpolator(new LinearInterpolator());                animator.setDuration(1000);                animator.addUpdateListener(new ValueAnimator.AnimatorUpdateListener() {                    @Override                    public void onAnimationUpdate(ValueAnimator valueAnimator) {                        int mSweepAngle = (int) valueAnimator.getAnimatedValue();                        Log.e(TAG, "onAnimationUpdate: " + mSweepAngle);                    }                });                animator.start();            }        });        mButton1.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                startActivity(new Intent(MainActivity.this, TestActivity.class));            }        });        mStretchableFloatingButton.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                mStretchableFloatingButton.startScroll();            }        });    }}